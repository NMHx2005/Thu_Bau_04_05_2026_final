SIGNAL LOST — manual QA matrix (trust T, clues C)

Reset: open index.html (prologue calls resetGame — clears trust, clues, flags, phrases, finalWords, notYetRound, dial decoy session).

Reset verification:
- After prologue, localStorage must not contain signalLost_finalWords or signalLost_notYetRound.
- sessionStorage signalLost_dialDecoyPassed cleared on new prologue.

Prologue chain (no game frame — fullscreen):
- index.html does NOT load gameFrame.js (avoids black-screen / clipped overlay).
- Overlay "Tap to start" covers full screen (z-index 9999).
- Slide 1 text visible immediately on index.html.
- After tap: rain loop starts (assets/audio/rain.mp3); sessionStorage signalLost_audioUnlocked=1.
- index.html: one click after unlock → prologue-2.html (resetGame on index only).
- prologue-2.html → prologue-3.html → prologue-4.html (title card) → waking-up.html (3 slides) → night0.html (4 slides + choice).
- prologue-2+ resume rain if signalLost_audioUnlocked=1.
- night0.html: "You sent it." / "You deleted it again." → addPhrase → night1.html.

Rain continuity:
- Prologue tap → hear rain before first slide text advance.
- night1.html: if audioUnlocked, rain auto-starts; if blocked, "Tap to hear rain" banner on explore.
- Rain fades when leaving for Night 2.
- resetGame does NOT clear signalLost_audioUnlocked (same browser session).

Bridge pages:
- continue-to-night2.html: trust 0-3 / 4-6 / 7+ flavour; click → night2.html.
- between-nights.html: clues>=3 and trust>=7 vs else; click → continue-to-night3.html.

Night order guards (no skip):
- night2.html: requires doneSignal1 (Night 1 signal puzzle completed). Else → night1.html.
- night3.html: requires foundHiddenThread (archived thread opened in Notes or Browser in Night 2). Else → night2.html.

Night 1 — exploration beats:
- Visit note before dial → extra Unknown line in chat.
- Visit window / laptop / photo / coat → each adds one conditional line (see night1Data.js).

Night 1 — dial / lightbox:
- dialBuf caps at 10 digits.
- ESC closes object lightbox; close button receives focus when opened.
- window/coat lightbox: "Read more →" → lore-window.html / lore-coat.html; Back → history.back().
- Correct digits only: 0427318247 (spaces ignored).
- Decoy first hint wrong digit; shake; then correct hint.
- Night 1 complete → continue-to-night2.html (not night2.html direct).

Night 1 — rain / decode:
- Rain audible on prologue after tap and on Night 1 explore (volume ~0.12).
- Rain fades when leaving for Night 2 (Continue after signal decode).
- Decode uses assets/images/LastLocation.png; falls back to Bedroom.png if missing.

Signal meter (status bar on all phone phases):
- Visible on night1 phase-phone, night2 (apps/memory/chat), night3.
- Starts empty (trust 0, clues 0) after prologue reset.
- Chat choice with positive trust → bars rise; negative → bars fall.
- tryAwardClue (signal decode, memory, hidden thread, heartbeat) → next fragment dot fills.
- Hover/title: "Connection X/10 · Fragments Y/4".
- Replay hidden thread / signal puzzle: meter clues must not increase twice.

Night 2 — hidden thread in-app:
- After free-text + UNKNOWN follow-up, phase returns to Apps with banner; "Continue to Night 3" when hidden found.
- Open Notes OR Browser → expand Drafts / Draft sync.
- First open awards hidden clue (tryAwardClue idempotent).
- Replay: opening thread again must not add a second hidden clue.

Voicemail:
- trust < 5 → locked copy.
- trust >= 5 → "Play message" plays assets/audio/voicemail.mp3 (or tone fallback).
- trust >= 5 → "Read transcript →" opens voicemail-transcript.html; Close → history.back().

Night 2 side pages (history.back):
- Notes → memory-draft.html.
- Browser → memory-east-entrance.html, memory-three-weeks.html.
- Night 2 exit (hidden thread open) → between-nights.html (not night3.html direct).

Night 3 word bank:
- Done: wordBuilt echoes quoted line (italic, 1.8s) before finOverlay.

Routing (Night 3 after word puzzle Continue):
- T>=7 and C>=3 → ending-shell.html?outcome=found
- T>=4 or C>=2 → ending-shell.html?outcome=static
- else → ending-shell.html?outcome=notyet

Ending FOUND (shell + legacy ending-found.html):
- line1 = word bank (getFinalWords).
- If getPhrases().length > 0: "you said, once: …" under finalEcho (night0 phrase or Night 2 free-text).
- Credits link appears ~9s → credits.html; Epilogue → after.html shows getFinalWords.

Puzzle signposting:
- Night 1 signal panel: hint "Slide until the picture is fully clear (100%)."
- Night 2 memory: hint "Drag each card into chronological order, then tap Lock order."

Assets:
- Bedroom blur.png compressed (display-sized; under 300KB target).

Boundary spot checks:
1) T=3, C=3 → static (C>=2)
2) T=7, C=2 → static (T>=4)
3) T=7, C=3 → found
4) T=3, C=1 → notyet

Clue idempotency:
- Replay Night 1 signal puzzle: clueCount must not increase twice (doneSignal1 flag).
- Hidden thread: only one clue (foundHiddenThread flag).

NOT YET (two-step):
- First screen: "Stay here (round 2)" sets notYetRound=1.
- "Enough (→ STATIC now)" → ending-shell.html?outcome=static.
- If notYetRound>=1 on load: single button to STATIC.

Audio:
- Pixabay MP3: rain, notification, typing, heartbeat, voicemail.
- Heartbeat puzzle uses heartbeat.mp3 (not only sine beep).
- Web Audio resume after prologue tap.

Accessibility:
- prefers-reduced-motion: skip eyelid blink; no dial shake; instant hotspot dots.

Mobile:
- Game frame fits viewport; dial/chat buttons min 44px touch target.
- No horizontal overflow on 375px width.

jQuery UI (Night 2):
- css/jquery-overrides.css loads after jquery-ui.css; sortable placeholder matches palette.

Document pages (scroll):
- credits.html, reference.html, after.html: body.doc-page scrolls inside game frame.
- memory-*.html, lore-*.html: body scrolls when content is tall.

Reference:
- reference.html, credits.html, and ATTRIBUTION.txt list third-party assets.
